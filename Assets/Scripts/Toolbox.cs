using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Toolbox : MonoBehaviour
{
    MeshGenerator mg = new MeshGenerator();

   // Mesh quad = mg.CreateQuad(new Vector3(4, 0, 2));
    //HalfEdge QuadsToHalfEdges() { };
}
